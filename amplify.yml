# AWS Amplify build spec - Static site (no build step)
# Your site is plain HTML/CSS/JS, so we just publish the repo root.
version: 1
frontend:
  phases:
    build:
      commands: []
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
